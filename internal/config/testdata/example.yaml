pullRequest:
  filters:
    sous-chefs-renovate:
      owner: sous-chefs
      type: org
      summaryRegex:
      raisedBy: renovate
      labels:
        - "type/renovate"
      repositories: # Optional filter for specific repos in larger orgs where we do not care for everything...
        - "hub-sphere"
